[project]
name = "sprintboard"
version = "0.1.0"
description = "A lightweight web task board with DevOps-ready delivery assets."
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
